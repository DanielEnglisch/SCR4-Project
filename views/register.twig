<?php self::renderView('partial/header', $model); ?>

<div class="container">
<h1>Register</h1>
    <?php self::beginActionForm('register','User',null,'post') ?>

        <div class="form-group">
            <label for="username">Username</label>
            <input class="form-control" type="text "id="username" name="un" value="<?php isset($model['username'])? self::htmlOut($model['username']):"" ?>"/>
        </div>

        <div class="form-group">
            <label for="password1">Password</label>
            <input type="password" class="form-control" id="password1" name="pwd1"/>
        </div>

        <div class="form-group">
            <label for="password2">Repeat Password</label>
            <input type="password" class="form-control" id="password2" name="pwd2"/>
        </div>

        <button class="btn btn-primary">Register</button>
    <?php self::endActionForm(); ?>
</div>

<?php self::renderView('partial/footer', $model); ?>
