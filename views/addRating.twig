<?php self::renderView('partial/header', $model); ?>

<div class="container">
<h1>Add Rating</h1>
    <?php self::beginActionForm('addRating','Ratings',null,'post') ?>

    <input type="hidden" name="pid" value="<?php self::htmlOut($model['pid']) ?>"/>


        <div class="form-group">
            <label for="comment">Comment</label>
            <input class="form-control" type="text" id="comment" name="comment" value="<?php isset($model['comment'])? self::htmlOut($model['comment']):"" ?>"/>
        </div>

      
         <div class="form-group">
            <label for="value">Value</label>

             <select class="form-control" id="value" name="value">
                <?php for($i = 1 ; $i <= 5; $i++): ?>
                
                    <?php if(isset($model['value']) && $i == $model['value']): ?>
                        <option  selected value="<?php self::htmlOut($i) ?>"><?php self::htmlOut($i) ?></option >
                    <?php else: ?>
                        <option  value="<?php self::htmlOut($i) ?>"><?php self::htmlOut($i) ?></option >
                    <?php endif; ?>

                <?php endfor; ?>
                
            </select> 

        </div>

        <button class="btn btn-primary">Add</button>
    <?php self::endActionForm(); ?>
</div>

<?php self::renderView('partial/footer', $model); ?>
